---
# This playbook uses the win_ping module to test connectivity to Windows hosts
- name: Ping 
  hosts: all 

  tasks:
  - name: whoami
    win_command: whoami
    
  - name: Install firefox
    win_chocolatey:
    name: firefox
    state: present
